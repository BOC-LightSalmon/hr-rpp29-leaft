describe('<Driver />', () => {
  it ('should pass', () => {
    expect(1).toEqual(1);
  });
});

describe('Map />', () => {
  it ('should pass', () => {
    expect(1).toEqual(1);
  });
});

describe('<RoutesList />', () => {
  it ('should pass', () => {
    expect(1).toEqual(1);
  });
});

describe('<RouteForm />', () => {
  it ('should pass', () => {
    expect(1).toEqual(1);
  });
});

describe('<Table />', () => {
  it ('should pass', () => {
    expect(1).toEqual(1);
  });
});