// import { render, screen } from '@testing-library/react';
// import App from './App';

// test('renders learn react link', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/leaft/i);
//   expect(linkElement).toBeInTheDocument();
// });

describe('dummy test', () => {
  it ('should pass', () => {
    expect(1).toEqual(1);
  });
});
